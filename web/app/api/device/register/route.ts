import { NextResponse } from 'next/server'; import { randomBytes } from 'node:crypto'; import { db } from '@/lib/db'; import { assertDeviceSecret } from '@/lib/auth';
export async function POST(req:Request){try{assertDeviceSecret(req);const body=await req.json();const d=await db();const found=await d.collection('devices').findOne({pairingCode:body.pairingCode});if(!found)return NextResponse.json({error:'Invalid pairing code'},{status:404});await d.collection('devices').updateOne({_id:found._id},{$set:{name:body.name||found.name,parentConnected:true}});return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:'Unauthorized'},{status:401})}}
export async function GET(){const d=await db();const devices=await d.collection('devices').find({}).sort({lastSeen:-1}).limit(100).toArray();return NextResponse.json({devices:devices.map(x=>({...x,_id:String(x._id),online:Date.now()-new Date(x.lastSeen??0).getTime()<15000}))})}
