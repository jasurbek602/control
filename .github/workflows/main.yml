name: Android Build

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build Family Guard APK
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: control/android-child

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Gradle 9.5.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.1'
          validate-wrappers: false

      - name: Show environment
        run: |
          java -version
          gradle --version
          pwd
          ls -la
          ls -la app

      - name: Clean build
        run: gradle clean --stacktrace --no-daemon

      - name: Build Debug APK
        run: gradle :app:assembleDebug --stacktrace --no-daemon

      - name: Verify APK
        run: |
          test -f app/build/outputs/apk/debug/app-debug.apk
          ls -lh app/build/outputs/apk/debug/app-debug.apk

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: familyguard-debug-apk
          path: control/android-child/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14

      - name: Upload build reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: familyguard-build-reports
          path: |
            control/android-child/app/build/outputs/**
            control/android-child/app/build/reports/**
          if-no-files-found: ignore
          retention-days: 7
